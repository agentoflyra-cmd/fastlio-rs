[package]
name = "fastlio-estimator"
version.workspace = true
edition.workspace = true

[dependencies]
fastlio-map = { path = "../fastlio-map" }
fastlio-types = { path = "../fastlio-types" }
nalgebra = { workspace = true }
